---
name: visual-presentation-generator
description: >-
  把一句话主题生成一套完整、可在浏览器打开的 HTML 演示文稿。云端负责生成整套
  材料包（HTML源码、内容大纲、设计配置、质检报告），本地负责把源码渲染成真实网页、
  执行两处人工确认（改稿 / 定风格）、校验每份产物，并交付。当用户想让主题或简报
  变成一份排好版的 HTML 演示文稿 / 幻灯片时使用。
---

# 视觉演示生成器

## 这个技能做什么
把主题生成一套自包含的 HTML 幻灯片，外加结构化大纲、设计配置与质检报告。一个主题 = 一份演示文稿。

## 开始之前：语言（跟随你装的 zip）

- zip 名带 `-zh` = 简体中文版 → 云端输出默认中文
- zip 名带 `-en` = English 版 → 云端输出默认英文

> 本地完整包功能一致；差别只在云端输出语言（跟随 zip）。
>
> 云端只调用胜算云 Shengsuanyun（LoomLoom），人民币扣费。**每次云端执行前会先明确告诉你本次预估费用（¥ 金额），经你确认后才会扣除。** 需数字/指标的页面必须由你提供素材，缺失一律用 `[待用户提供]` 占位，云端不编造。

## 职责边界
- **云端（LoomLoom）只负责内容生产。** 它返回的是把你演示文稿的 **HTML 源码字符串**（text/plain）。云端不能渲染、预览、截图、打包，也不能做人工编辑。
- **本地（本技能 + 你）负责交付呈现。** 你把 HTML 源码渲染成真实的 `.html`，打开预览，执行两处人工确认（改稿 / 定风格），校验所有产物，并交付/汇报。**两处人工确认无法放在云端。**

## 平台现实（权威）
- LoomLoom 模型步骤：仅文本 / 图片 / 视频 / 音频 / 3D，无代码 / 浏览器步骤。
- `capability resolve` 只接受 text / audio / video / image 输出。
- HTML 以 `text/plain`/`.txt` 源码字符串交付；请本地位可达渲染和预览。
- 无流式，因此所有确认都由本地编排。

## 工作流（报价 → 确认 → 跑 → 收尾）

1. **一句话说明产出** —— 告诉用户会收到什么：
   "这个主题 → 你会收到一套可编辑 HTML 演示文稿（可预览可改）。"
2. **分步收敛输入（一次一问）** —
   依次只问：`topic`（必填）→ `audience`（可选）→ 页数/风格（可选）→ 素材/链接（可选）。一次只问一个。
3. **本地预检（不花钱）** —— 确保 `topic` 非空、页数 ≤ 30。
3b. **幻觉风险预筛（花钱前强制执行 · 不花钱只耗 CPU）** —— 用 `scripts/preflight.py --topic "<主题>" --reference "<素材>" --json` 在**报价之前**筛查：
   - **RED（exit 2）**：主题含数字/业绩/市场/融资类需求，但 `reference` 为空 → **禁止报价、禁止提交**。必须先让用户补素材，或明确同意用 `[待用户提供]` 占位。
   - YELLOW（exit 1）：有数字线索但缺素材 → 提示用户，请其确认后再继续。
   - GREEN（exit 0）：可进入报价。
   - **绝不带着“大概率编造数据”的输入进云端花钱。**
4. **报价 → 明确确认（花钱节点）**
   ```
   本次：1 份演示 ≈ ¥Y（当前不发布，只收模型成本）
   会调用云端生成（人民币）。
   确认执行吗（是 / 否）
   ```
   未获得明确确认前不运行；请用户回复“确认”。
5. **选择风格模板（跑云端前必问）** —— 在提交云端任务之前，**必须先向用户询问用哪个视觉模板/风格**：
   - 展示当前可用风格：默认 `modern`，以及 25 个命名风格键（如 `linear`、`aesop`、`apple-hig`、`muji-kenya-hara`、`stripe-press`、`vercel-mesh`…，完整见 `references/style-catalog.json`）。
   - 明确问：**「这份演示文稿要用哪个风格模板 / 视觉风格？」** 给 2–4 个贴合主题的建议选项 + “其它（自由输入风格键或描述）”。
   - **未获得用户明确选择前不运行云端。**
   - 用户选好后，把该风格键写入 `style` 列（默认 `linear`）。
6. **云端跑一次** —— 通过 Loom 模板 `template-spec run/submit-workbook`，模板与版本见 `references/contracts.md`。1 行 = 1 份演示文稿。
7. **本地渲染 + 校验（不花钱）** —— 取回 compile 产物（HTML 源码），剥离代码围栏写成真实 `.html`，打开预览；执行 `scripts/validate_presentation.py`（结构/完整/主题/可用性）。
   - 若校验不通过 → 报告失败点，回本地改造/重跑，**绝不交付占位稿**。
   - **Checkpoint 1 改稿**：展示大纲与文案，请用户确认或修改；若修改，用修改后输入重新走。
   - **Checkpoint 2 定风格**：若风格仍可再调，展示备选风格方向；用户选定 → 按新设置重新生成。
8. **交付 + 摘要**：给 `presentation.html`（可打开）、`design_config`、`content_schema`、`qa_report`；
   摘要：在哪里、耗时、成本（多次调用就报累计）。不暴露 token/内部 ID/命令。

## 交付前的质量门（强制）
- 每份产物在交付前都由 `scripts/validate_presentation.py` 校验，所有项 **通过才交付**。
- 未通过 → 返回「卡在哪、需要你给什么」，修复后重跑，再交付。
- 拒绝不支持的 / 伪造的 / 无法对照的结论；数字只能来自用户输入或显式标 `[由你补充]`。

## 参考
- `references/contracts.md` —— 云端模板/版本、输入字段、输出 schema
- `references/validation.md` —— 校验与阈值
- `references/schema.md` —— 四份产物的目标结构
- `agents/openai.yaml` —— 用作 metadata

## 注意事项
- 对外交流用中文（允许夹英文产品名）；产物文件名与代码用英文。
- 不发布到 Market，除非用户明确要求。